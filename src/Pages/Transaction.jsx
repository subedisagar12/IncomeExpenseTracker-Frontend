import React from "react";
import ItemsList from "../Components/List";
const Transaction = ({ addOpen }) => {
  return (
    <>
      <ItemsList addOpen={addOpen} />
    </>
  );
};
export default Transaction;
